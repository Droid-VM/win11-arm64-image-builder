# === win11-arm64-image-builder 設定範例 ===
# 複製成 .env：  cp .env.example .env
# 腳本（linux/make-patches.sh、macos/build.sh/02/03）啟動時會自動載入 .env。
# 已在環境/CLI 設定的變數優先，.env 只補沒設定的。.env 已被 .gitignore 排除。

# --- Win11 ARM64 ISO ---
# 路徑；留空則自動抓 inputs/ 下第一個 *.iso
# SRC_ISO=inputs/win11_arm64.iso

# --- 驅動來源（三種寫法擇一）---
#   1) GitHub repo URL  -> 自動下載該 repo 'dev' release 的驅動 zip（預設）
#   2) 本地 .zip 路徑   -> 自動解壓
#   3) 本地資料夾路徑   -> 每個驅動一個子資料夾
# 私有 repo 需先 gh auth login；公開 repo 用 curl 即可。
DRIVERS_DIR=https://github.com/HuJK-Data/gunyah-guest-drivers-windows

# --- 憑證（通常免設，會從驅動 .cat 自動萃取簽章者）---
# CERT=inputs/DroidVM_Test.cer

# --- 進 WinPE 的開機驅動（空白分隔）---
# WINPE_DRIVERS="viostor vioscsi"

# --- 安裝/qemu 參數 ---
# MEM=8G
# SMP=6
# DISK_SIZE=40G
# DISPLAY_OPT=-vnc 127.0.0.1:5
